#include <iostream>
#include <cmath>

int main(){

    double x=3;
    double y=4;
    double z;

    // z = std::max(x, y);
    // z = std::min(x, y);

    // z = pow(2, 3);
    // z = sqrt(9);
    // z = abs(-5);

    // z = round(3.1415);
    // z = ceil(3.1415);
    // z = floor(3.1415);

    std::cout << z << std::endl;


    return 0;
}